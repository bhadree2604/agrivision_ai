// AgriVision AI – Runtime Configuration
// ─────────────────────────────────────────────────────────────────────────────
// Set your Plant.id API key here for local development.
// On Netlify, set PLANT_ID_API_KEY as an environment variable in Site Settings
// → Build & Deploy → Environment Variables. Netlify's snippet injection will
// replace the placeholder below automatically when using the netlify.toml
// build command, or you can set this value directly for local testing.
//
// IMPORTANT: Do NOT commit a real API key to version control.
// Keep this file in .gitignore when using a real key locally.
// ─────────────────────────────────────────────────────────────────────────────
window.PLANT_ID_API_KEY = '';  // Replace with your key for local dev
